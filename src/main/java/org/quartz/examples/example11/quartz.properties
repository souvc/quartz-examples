
#============================================================================
# 调度器主要属性配置
# 这些属性定义了调度器的基本行为和标识信息
#============================================================================

# 调度器实例名称，用于标识调度器实例
org.quartz.scheduler.instanceName: TestScheduler

# 调度器实例ID，AUTO表示自动生成唯一ID
org.quartz.scheduler.instanceId: AUTO

# 跳过版本更新检查，避免启动时的网络请求
org.quartz.scheduler.skipUpdateCheck: true

#============================================================================
# 线程池配置
# 线程池控制同时执行的任务数量，是性能调优的关键配置
#============================================================================

# 线程池实现类，SimpleThreadPool是最常用的实现
org.quartz.threadPool.class: org.quartz.simpl.SimpleThreadPool

# 线程池大小，决定了最多可以同时执行多少个任务
# 本示例设置为12，意味着最多12个任务可以并发执行
org.quartz.threadPool.threadCount: 12

# 线程优先级，范围1-10，5为普通优先级
org.quartz.threadPool.threadPriority: 5

#============================================================================
# 任务存储配置
# JobStore负责存储任务和触发器信息
#============================================================================

# 任务错过触发的阈值时间（毫秒）
# 如果任务超过这个时间才执行，会被认为是错过触发
org.quartz.jobStore.misfireThreshold: 60000

# 使用内存存储，适合演示和测试
# 重启后所有任务信息会丢失
org.quartz.jobStore.class: org.quartz.simpl.RAMJobStore

# 以下是数据库存储的配置示例（已注释）
# 生产环境建议使用数据库存储以保证任务持久化
#org.quartz.jobStore.class: org.quartz.impl.jdbcjobstore.JobStoreTX
#org.quartz.jobStore.driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
#org.quartz.jobStore.useProperties: false
#org.quartz.jobStore.dataSource: myDS
#org.quartz.jobStore.tablePrefix: QRTZ_
#org.quartz.jobStore.isClustered: false

#============================================================================
# 数据源配置（仅在使用数据库存储时需要）
# 当使用 JobStoreTX 或 JobStoreCMT 时需要配置数据源
#============================================================================

# 数据库驱动类
#org.quartz.dataSource.myDS.driver: org.postgresql.Driver

# 数据库连接URL
#org.quartz.dataSource.myDS.URL: jdbc:postgresql://localhost/dev

# 数据库用户名
#org.quartz.dataSource.myDS.user: jhouse

# 数据库密码
#org.quartz.dataSource.myDS.password: 

# 最大连接数
#org.quartz.dataSource.myDS.maxConnections: 5

